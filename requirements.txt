# Vercel deployment dependencies (lightweight - PyTorch/Ultralytics excluded due to 250MB bundle limit)
# /api/measure is proxied to the HuggingFace Space inference backend (POINTSX_INFERENCE_ENDPOINT).
# /api/measure/mock and /api/tts are served locally on Vercel.
fastapi>=0.115.0
pydantic>=2.0
numpy>=1.26.0,<2
python-multipart>=0.0.26
edge-tts>=7.0.0
httpx>=0.27.0
