# Tasks opened 2026-09-22

Status lives here until each is closed; ledger numbers stay local (`runs/eval/ledger.jsonl`).

## T1. Resolve the two unlinked photo pairs from the university session

Context: 2026-09-22 session, 24 dataset rows = 10 with real values + 14 "all-1s" rows that are
extra photo pairs of already-measured people (dotted measurement suit = tight, own clothes = loose).
Links for 24 pairs / 9 people are recorded in `supabase-dump/gt_links.json` (local, gitignored);
Telegram pairs copied to `supabase-dump/tg-A-1/`, `tg-D-1/`.

Open:
- [ ] `3aa65dfb…` (09:00, suit, F 168/21, second room). Values near person A (`bcc92531`) but not
      equal (chest 81 vs 84, back width 29 vs 33, sleeve 62 vs 65, outer 105 vs 103).
      **Ask:** same person re-measured (→ keep as A, second GT set = σ_GT estimate) or a new person K?
- [ ] `dc6faaf0…` (08:53, own clothes, F 165/19). No measurements in any row.
      **Ask:** whose photos, and the 17 values (paper/Telegram?). If unknown → drop the pair.

Then (code, ~half a day):
- [ ] `scripts/build_eval_csv.py`: read `gt_links.json`; a pair whose row is a placeholder takes GT
      from its `gt_row`; add columns `person`, `clothing`; external folders (`tg-*`) become rows.
- [ ] `pointsx-eval` / `eval_track.py`: report per-person (not per-pair) aggregates and a
      suit-vs-own split — the first real tight/loose comparison on the same bodies.
- [ ] `eval_track.py run --label "corpus +N persons, suit/own pairs"` → ledger #8; update
      `accuracy-approaches-survey.md` §1a with the new corpus size (16 → ~25 persons, still 4 men).

## T2. Build the license-clean synthetic dataset

Plan and audit: `docs/synthetic-dataset-audit-plan.md` (§4 design, §5 gates, §6 steps).
Record of what is already measured that the dataset must beat: survey §1a (ledger #0-#7).

- [ ] **Step 0 — HBW baseline** (eval-only, NC): register at shapy.is.tue.mpg.de, download HBW;
      `eval/hbw.py` runs the *production* pipeline on the photos; third column in the ledger.
      Output: production MAE on HBW (height/chest/waist/hips, scan GT). Blocks everything below.
- [ ] **Step 1 — cleanup** (list before deleting): `data/synthetic-pose/` (6.4 GB) +
      `data/synthetic-pose.zip` (2.5 GB) + `data/synthetic-seg/` (172 MB) — wrong GT, SMPL-X-tainted;
      `assets/textures/smplitex/` (106 MB); `data/eval/photos/` (no GT). Write `assets/LICENSES.md`.
- [ ] **Step 2 — body model smoke test**: 20 bodies each with MPFB2 (Blender add-on, outputs CC0)
      and Anny (Apache 2.0); GT from mesh via vendored anthropometry (remap landmarks); gate 1
      (per-sex means within 2 cm of ANSUR, `gt_sanity` ≥ 99 %). Decide.
- [ ] **Step 3 — clothing + masks**: Blender cloth modifier, tight/loose regimes, CC0 garments;
      emit clothed mask + nude-body mask + 16 keypoints; phone-like camera; gate 2 on 50 bodies
      (synthetic over-read must match real: waist ≈ 1.2×, hip ≈ 1.07×, thigh ≈ 1.12×, chest ≈ 1.03×).
- [ ] **Step 4 — pilot 500 + segmenter**: train body-under-clothing seg on synthetic only; gate 3 =
      improves on HBW and does not regress app GT (`eval_track.py`), now also checkable on the
      suit-vs-own pairs from T1.
- [ ] **Step 5 — scale to 5k** only after gate 3.

Parallel, not code: measured cohort (20-30 people, ISO 8559-1, tight clothing, weight); more men.
